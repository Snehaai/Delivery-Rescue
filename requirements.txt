# Delivery Rescue Backend — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Agentic AI orchestration
langgraph==0.2.28
langchain-core==0.3.8

# LLM - Claude API
anthropic==0.34.2

# HTTP client for OSM Nominatim
httpx==0.27.2

# Data validation
pydantic==2.9.2

# Audio transcription (optional — install separately if needed)
# openai-whisper==20240930

# Audio processing helper (used by whisper)
# ffmpeg must be installed system-wide: brew install ffmpeg / apt install ffmpeg

# File uploads
python-multipart==0.0.12

# WebSocket support (included in uvicorn[standard])
websockets==13.1